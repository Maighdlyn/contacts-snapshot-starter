<%- include partials/header.ejs %>

  <div class="contact-show-page-controls">
    <% if (role === 'admin') { %>
      <a class="delete-contact" href="/contacts/delete/<%= contact.id %>">delete</a>
    <% } %>
  </div>

  <h1><%= contact.first_name %>&nbsp;<%= contact.last_name %></h1>

<%- include partials/footer.ejs %>
